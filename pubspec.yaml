name: Flutter Investment Performance Calculator

description: A Flutter application for managing and analyzing investment performance.

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.0
  charts_flutter: ^0.12.0
  sqflite: ^2.0.0
  path_provider: ^2.0.0
  pdf: ^3.6.0
  excel: ^2.0.0
  animations: ^2.0.0
  flutter_localizations:
    sdk: flutter
  ... # Add more dependencies as needed

flutter:
  uses-material-design: true
